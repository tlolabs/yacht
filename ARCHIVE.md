# Legacy archive — 2.0.2

This branch preserves the complete repository before removal of the original Tk/Python application and obsolete packaging. It is an archival snapshot, not a released product.

- Frozen original application: `legacy-python/` (baseline `703bcd2`).
- Original regression snapshots: `50ba807`.
- Previous Swift implementation: `ec96ba2` (available in this branch history).
- Rust/native pre-archive implementation: `3cd82f8`.

Continue development on `main`. Existing data-format compatibility and frozen regression fixtures remain part of the current Rust/native implementation.
